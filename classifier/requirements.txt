# ── Core ML stack ──────────────────────────────────────────────────────────────
# RTX 5090 (Blackwell sm_120) requires torch >= 2.6
# Install CUDA 12.8 first: https://developer.nvidia.com/cuda-downloads
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# ── Model library ──────────────────────────────────────────────────────────────
timm>=1.0.9                  # EfficientNet-B0, MobileNetV2, pretrained weights

# ── Dataset acquisition ────────────────────────────────────────────────────────
datasets>=2.20.0             # HuggingFace datasets (LAION streaming)
huggingface-hub>=0.23.0
img2dataset>=1.41.0          # Parallel image downloader for LAION URLs

# ── Data pipeline ──────────────────────────────────────────────────────────────
Pillow>=10.3.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0          # train/val split, class weights, metrics
tqdm>=4.66.0
requests>=2.31.0
aiohttp>=3.9.0               # async URL downloading

# ── Training utilities ─────────────────────────────────────────────────────────
wandb>=0.17.0                # experiment tracking (optional but recommended)
torchmetrics>=1.4.0          # precision/recall/F1
matplotlib>=3.9.0            # confusion matrix plots
seaborn>=0.13.0

# ── Export: iOS (CoreML) ───────────────────────────────────────────────────────
coremltools>=8.0.0

# ── Export: Android (TFLite via ONNX) ─────────────────────────────────────────
onnx>=1.16.0
onnxruntime>=1.18.0
onnx-simplifier>=0.4.35
# ai-edge-torch>=0.3.0       # Google's direct PyTorch→TFLite (uncomment if preferred)
# tensorflow>=2.16.0         # needed for TFLite conversion via ONNX path
